# M2.6 — Функции глубже (замыкания, декораторы)

Модуль посвящён углублённому пониманию функций в Python: замыканиям и декораторам. Основная цель — научиться **осознанно
** писать декораторы, понимать их внутреннюю механику, порядок применения и корректно использовать их как с обычными
функциями, так и с методами классов.

---

## 🎯 Что ты освоишь

* как Python хранит состояние функций
* что такое замыкания и где они применяются
* как функции могут возвращать функции
* что такое декоратор на самом деле
* как работает синтаксис `@decorator`
* декораторы без аргументов и с аргументами
* порядок применения нескольких декораторов
* сохранение метаданных функций
* стандартные декораторы `wraps` и `lru_cache`
* применение декораторов к методам классов
* типичные ошибки и архитектурные ловушки

---

## 📘 Полная теория

### 1. Замыкания

**Замыкание** — это функция, которая:

* определена внутри другой функции
* использует переменные из внешней области видимости
* сохраняет эти переменные даже после завершения внешней функции

Пример:

```python
def make_adder(n: int):
    def add(x: int) -> int:
        return x + n

    return add
```

```python
add5 = make_adder(5)
add5(10)  # 15
```

Здесь:

* `n` — внешняя переменная
* функция `add` хранит ссылку на `n`
* состояние живёт рядом с объектом функции

Для изменения внешней переменной используется `nonlocal`:

```python
def make_counter():
    count = 0

    def inc():
        nonlocal count
        count += 1
        return count

    return inc
```

---

### 2. Что такое декоратор

**Декоратор** — это:

> функция, которая принимает функцию и возвращает новую функцию

Простейшая форма:

```python
def decorator(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs)

    return wrapper
```

Декораторы напрямую основаны на замыканиях:

* `wrapper` использует `func`
* `func` сохраняется во внешней области видимости

---

### 3. Декоратор без синтаксического сахара

Любой декоратор можно применить вручную:

```python
wrapped = decorator(func)
```

Синтаксис:

```python
@decorator
def func():
    ...
```

эквивалентен:

```python
func = decorator(func)
```

---

### 4. Порядок применения декораторов

```python
@d1
@d2
def f():
    ...
```

Разворачивается как:

```python
f = d1(d2(f))
```

Пример:

```python
def d1(func):
    def w():
        print("d1 before")
        func()
        print("d1 after")

    return w


def d2(func):
    def w():
        print("d2 before")
        func()
        print("d2 after")

    return w
```

Вывод при вызове `f()`:

```
d1 before
d2 before
f
d2 after
d1 after
```

Порядок декораторов **меняет поведение программы**.

---

### 5. Декораторы без аргументов

Примеры задач:

* логирование вызовов
* замер времени выполнения
* печать аргументов и результата

Пример логгера:

```python
def logger(func):
    def wrapper(*args, **kwargs):
        print(func.__name__, args, kwargs)
        return func(*args, **kwargs)

    return wrapper
```

Ключевое правило — использовать `*args, **kwargs`.

---

### 6. Декораторы с аргументами

Декоратор с аргументами — это **фабрика декораторов**:

```python
def repeat(n):
    def decorator(func):
        def wrapper(*args, **kwargs):
            for _ in range(n):
                result = func(*args, **kwargs)
            return result

        return wrapper

    return decorator
```

Использование:

```python
@repeat(3)
def greet(name):
    print(name)
```

Аргументы декоратора обрабатываются **один раз**, при декорировании.

---

### 7. Сохранение метаданных функции

При декорировании теряются:

* `__name__`
* `__doc__`
* сигнатура функции

Решение:

```python
from functools import wraps


@wraps(func)
def wrapper(...):
    ...
```

`wraps` — обязательная практика.

---

### 8. Стандартный декоратор `lru_cache`

`lru_cache` кеширует результаты вызовов функций по аргументам.

Пример:

```python
@lru_cache
def slow_square(x):
    time.sleep(1)
    return x * x
```

Инструменты:

* `cache_info()`
* `cache_clear()`

---

### 9. Декораторы и методы классов

Метод класса — это обычная функция, где `self` автоматически передаётся первым аргументом.

```python
class User:
    @logger
    def greet(self, name):
        return f"Hello {name}"
```

Для декоратора `self` — это просто `args[0]`.

Правильный декоратор:

* использует `*args, **kwargs`
* работает и с функциями, и с методами

---

## 📁 Структура модуля

```
M02_Advanced_Python/
└── 06_functions_deep/
    ├── task01_make_adder.py
    ├── task02_formatter_prefix.py
    ├── task03_counter_nonlocal.py
    ├── task04_make_fib.py
    ├── task05_message_builder.py
    ├── task06_simple_logger.py
    ├── task07_timeit_decorator.py
    ├── task08_repeat.py
    ├── task09_validate_range.py
    ├── task10_lru_cache_basic.py
    ├── task11_method_logger.py
    ├── task12_count_calls.py
    └── README.md
```

---

## 🧩 Задания

* замыкания и хранение состояния
* декораторы без аргументов
* декораторы с аргументами
* стандартные декораторы
* декораторы для методов классов

---

## ▶️ Как запускать

```bash
python taskXX_name.py
```

---

## ✅ Критерии готовности

* понятны замыкания и их поведение
* ты умеешь писать декораторы всех типов
* понимаешь порядок применения декораторов
* используешь `wraps`
* понимаешь работу `lru_cache`
* декораторы корректно работают с методами классов

---

## 🛟 Устранение неполадок

* предупреждения IDE про `self` — ограничение статического анализа
* `Parameter 'self' unfilled` — не ошибка Python
* проблемы часто связаны с порядком декораторов

---
