# M2.3 — Классы: базовая архитектура

В этом шаге ты учишься строить корректные, надёжные и архитектурно
сильные классы: с инвариантами, приватными атрибутами, property,
setter'ами, вычисляемыми свойствами и красивыми `__str__`/`__repr__`.

## 🎯 Что ты освоишь

-   `__init__`: создание валидного объекта
-   Инварианты
-   Приватные атрибуты (`_width`, `_age`, `_balance`)
-   `@property`: публичный API класса
-   setter'ы
-   Read-only свойства
-   Вычисляемые свойства
-   `__repr__`
-   `__str__`
-   Uniform Access Principle

## 📘 Полная теория

### 1. `__init__`: объект должен быть валидным сразу

``` python
class Segment:
    def __init__(self, length: float):
        if length <= 0:
            raise ValueError("Длина должна быть > 0")
        self._length = length
```

### 2. Инварианты

Инвариант --- это правило, которое должно быть истинно всегда.

-   возраст \> 0\
-   ширина/высота \> 0\
-   баланс ≥ 0\
-   номер счёта --- только цифры\
-   шкала ∈ {C, F}

Инварианты должны выполняться:

-   в `__init__`
-   в setter'ах

### 3. Приватные атрибуты

Используем:

    self._value

Чтобы скрыть реализацию и обеспечить стабильный публичный интерфейс.

### 4. `@property`

``` python
@property
def area(self):
    return self.width * self.height
```

### 5. Setter

``` python
@value.setter
def value(self, v):
    if v <= 0:
        raise ValueError
    self._value = v
```

### 6. Типы property

-   read-only\
-   со setter\
-   вычисляемые (area, celsius, perimeter)

### 7. `__repr__`

``` python
def __repr__(self):
    return f"Rectangle(width={self.width}, height={self.height})"
```

### 8. `__str__`

``` python
def __str__(self):
    return f"{self.username}, {self.age} y.o."
```

### 9. Uniform Access Principle

Используем:

    self.width
    self.height

Не используем:

    self._width

### 10. Паттерн "здорового класса"

``` python
class Entity:
    def __init__(self, value):
        self.value = value

    @property
    def value(self):
        return self._value

    @value.setter
    def value(self, v):
        if v <= 0:
            raise ValueError
        self._value = v

    def __repr__(self):
        return f"Entity(value={self.value})"

    def __str__(self):
        return f"value = {self.value}"
```

## 📁 Структура модуля

    M02_OOP_Files_Typing/
    └── 03_classes_base/
        ├── task01_box.py
        ├── task02_temperature.py
        ├── task03_user.py
        ├── task04_rectangle.py
        ├── task05_bank_account.py
        └── README.md

## 🧩 Задания

### Task 01 --- Box

### Task 02 --- Temperature

### Task 03 --- User

### Task 04 --- Rectangle

### Task 05 --- BankAccount

## ▶️ Как запускать

    python task01_box.py
    python task02_temperature.py
    ...

## ✅ Критерии готовности

-   объект всегда валиден\
-   setter'ы повторяют инварианты\
-   используются property\
-   корректные `__repr__` и `__str__`\
-   красивый, читаемый код

## 🛟 Устранение неполадок

Если setter принимает неверные данные --- проверь инвариант.

Если repr некорректен --- должен использовать property.

Если объект создаётся с ошибкой --- значит инварианты работают.
