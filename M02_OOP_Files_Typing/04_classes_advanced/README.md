# M2.4 — Классы: продвинуто (наследование, композиция, classmethod, staticmethod)

В этом шаге ты переходишь от базовой архитектуры классов к созданию
сложных, гибких, расширяемых объектных систем. Ты научишься правильно
использовать наследование, композицию, делегирование, методы класса,
статические методы и проектировать цепочки взаимодействий между
объектами.

## 🎯 Что ты освоишь

- Наследование как инструмент специализации поведения
- Правильное переопределение методов
- super() и цепочки инициализации
- MRO — порядок разрешения методов
- Композицию как основной инструмент построения систем
- Делегирование ответственности
- classmethod: фабрики, альтернативные конструкторы
- staticmethod: утилиты внутри класса
- Классовые атрибуты и маркеры стратегий
- Архитектуру «сервис + зависимости»

## 📘 Полная теория

### 1. Наследование: когда и зачем

Наследование — это отношение *«является»* (is‑a).

- Dog **является** Animal
- Admin **является** User
- Square **является** Rectangle

Когда наследование уместно:

1. Есть общий интерфейс/поведение
2. Поведение наследников расширяется или уточняется
3. Базовый класс описывает абстракцию

Когда наследование **не нужно**:

- чтобы «просто взять код»
- если нет отношения *is‑a*
- если объект должен «иметь» другой объект
  (тогда используется **композиция**)

Переопределение:

```
class Animal:
    def sound(self):
        return "sound"

class Dog(Animal):
    def sound(self):
        return "woof"
```

### 2. super() и расширение поведения

super() позволяет:

- вызывать методы родителя
- расширять, а не заменять его поведение
- корректно работать при множественном наследовании

Пример:

```
class Logger:
    def log(self, msg):
        return f"[LOG] {msg}"

class TimestampLogger(Logger):
    def log(self, msg):
        base = super().log(msg)
        return f"[TIME] {base}"
```

### 3. MRO — порядок разрешения методов

Python использует C3‑линеаризацию.

При вызове метода поиск идёт по цепи:

```
Child → Parent1 → Parent2 → ... → object
```

Посмотреть порядок:

```
Child.__mro__
```

### 4. Композиция — главный инструмент инженера

Композиция = объект **имеет** другой объект.

Примеры:

- машина **имеет** двигатель
- сервис **имеет** логгер
- пользователь **имеет** профиль

Композиция обеспечивает:

- слабую связанность
- лёгкое тестирование
- расширяемость
- заменяемость зависимостей

Всё это делает композицию более практичной, чем глубокие иерархии.

### 5. Делегирование

Делегирование — это перенос части работы на вложенный объект:

```
class Service:
    def __init__(self, logger):
        self.logger = logger

    def run(self):
        self.logger.log("start")
```

Service не знает *как* работает логгер — и это правильно.

### 6. classmethod — фабрики

Метод класса:

- принимает cls
- создаёт объект через cls(...)
- гибок к наследованию

Используется для:

- альтернативных конструкторов
- создания объектов из строк, dict, JSON
- фабрик заранее настроенных объектов

Пример:

```
@classmethod
def from_string(cls, raw):
    name, email = raw.split("|")
    return cls(name, email)
```

### 7. staticmethod — утилиты внутри класса

Используется, когда метод:

- не использует self
- не использует cls
- логически относится к классу

Пример:

```
@staticmethod
def normalize(text):
    return text.strip().lower()
```

staticmethod — редкий инструмент.
Если метод не привязан к классу концептуально — лучше вынести на уровень модуля.

### 8. Классовые атрибуты — «маркеры стратегий»

```
class EmailSender(Sender):
    sender_type = "email"
```

Это называется:

- **маркер стратегии** (strategy marker)
- **type tag**
- **class metadata**

Используется для выбора поведения без isinstance.

### 9. Паттерн «сервис + зависимости»

Типичная архитектура:

- сервис делает высокоуровневую работу
- отправители, логгеры, билдеры — внедряются через композицию
- сервис делегирует им работу

Это лучший способ проектировать гибкие и расширяемые системы.

---

## 📁 Структура модуля

```
M02_OOP_Files_Typing/
└── 04_classes_advanced/
    ├── task01_basic_inheritance.py
    ├── task02_super_and_override.py
    ├── task03_super_init_user.py
    ├── task04_user_profile_composition.py
    ├── task05_notification_strategy_composition.py
    ├── task06_user_from_string.py
    ├── task07_user_factory_validation.py
    ├── task08_notification_full.py
    └── README.md
```

## 🧩 Задания

### Task 01 — Basic Inheritance
Наследование, переопределение методов.

### Task 02 — super() and Override
Расширение поведения родителя.

### Task 03 — super().__init__
Расширение инициализации в дочернем классе.

### Task 04 — User + Profile (композиция)
Отношение has‑a и делегирование.

### Task 05 — Notification (композиция + Protocol/Union)
Делегирование поведения отправителю.

### Task 06 — User.from_string (classmethod)
Альтернативный конструктор.

### Task 07 — staticmethod + classmethod
Разделение ответственности: валидация и фабрика.

### Task 08 — Notification System v2
Композиция + наследование + фабрики + утилиты — финальный проект.

## ▶️ Как запускать

```
python task01_basic_inheritance.py
python task02_super_and_override.py
...
python task08_notification_full.py
```

## ✅ Критерии готовности

- корректное наследование
- super() используется по назначению
- композиция реализована осознанно
- классы слабо связаны
- есть делегирование
- classmethod создаёт объекты корректно
- staticmethod выполняет утилитарные функции
- код чистый, читаемый и архитектурно устойчивый

## 🛟 Устранение неполадок

**super() вызывает ошибку**
→ Проверь порядок классов и сигнатуры методов.

**Наследник не использует поведение родителя**
→ Убедись, что вызвал super().

**Композиция не работает**
→ Убедись, что передаёшь объект, а не класс.

**builder.from_sender даёт UNKNOWN**
→ Проверь sender_type у отправителя.

**from_string падает с ошибкой**
→ Разбивай строку безопасно и проверяй длину.

---
