# M2.1 — Исключения и Error Design

Короткая цель модуля: научиться проектировать исключения как инструмент инженерии. Мы учимся различать технические и
доменные ошибки, превращать низкоуровневые ошибки в смысловые (через `from error`), и маппить доменные ошибки в UX/CLI (
exit codes, сообщения). Все задания выполняем по **нашему микро-циклу** ✅

## 🎯 Что ты освоишь

- Разделение ошибок на технические (I/O) и доменные.
- Создание иерархии своих ошибок (BaseError → SpecificError).
- Перевод стандартных ошибок в доменные (`from error`).
- Разделение зон ответственности по слоям (I/O, Parser, Boundary).
- Дизайн API: какие ошибки функция обещает наружу (контракт).
- Дизайн UX/CLI: «исключение → сообщение → exit code → log level».

## 📘 Полная теория

### 1. Зачем нам дизайн ошибок

Исключения — это не “авария в коде”.
Исключение — это **сигнал о невозможности продолжить логику**.

Дизайн ошибок нужен для:

- читабельности
- изоляции областей ответственности
- предсказуемых API
- отслеживания первопричин

Хаотичные исключения → хаос в коде.
Доменные исключения → чистые границы.

### 2. 2 семейства ошибок

| Семейство            | Где возникает             | Примеры                                  |
|----------------------|---------------------------|------------------------------------------|
| Технические (system) | IO / JSON / формат        | `FileNotFoundError`, `JSONDecodeError`   |
| Доменные (business)  | правила нашего приложения | `LimitNotPositive`, `MinAmountViolation` |

**Тех** ошибки мы переводим в **доменные**, чтобы верхний слой не зависел от внутренних деталей.

### 3. Цепочка исключений

Перевод делается так:

```python
except ValueError as error:
raise MyDomainError(...) from error
```

`from error` — критично.
Это сохраняет первопричину.
Без этого мы теряем источник сбоя.

### 4. Границы слоёв

| Слой             | Что там делаем?                                            |
|------------------|------------------------------------------------------------|
| I/O              | ловим технические исключения + переводим в доменные        |
| Parser/Validator | проверяем структуру и бизнес-правила → поднимаем доменные  |
| Boundary/CLI     | НЕ переводим — только показываем UX и выставляем exit code |

### 5. Контракт API

Публичная функция должна **декларировать список исключений**, которые она может поднять.

Это делает использование **предсказуемым** и стабильным.

## 📁 Структура модуля

```
M02_OOP_and_Design/
└─ 01_exceptions_error_design/
   ├─ task01_error_levels.py
   ├─ task02_error_contract.md
   ├─ task03_error_boundaries.md
   ├─ task04_error_chaining.md
   ├─ task05_payment_rules.py
   └─ task06_cli_mapping.md
```

## 🧩 Задания

**Task 01 — Error Levels**
Разделение технических и доменных ошибок, проектирование иерархии.

**Task 02 — API Contract**
Документируем какие исключения публичная функция обещает наружу.

**Task 03 — Boundaries**
Определяем на каком уровне ловить / переводить / показывать.

**Task 04 — Chaining**
Проверка, что `from error` присутствует во всех переводах.

**Task 05 — Payment Rules**
Мини-домен на правилах: amount > MIN_PAYMENT. Доменные ошибки.

**Task 06 — CLI Mapping**
Таблица «исключение → user message → exit code → log level».

## ▶️ Как запускать

Просто запускать `.py` файлы по задачам.

mypy / pytest в этом шаге **не требуются**.
Фокус — **дизайн** и **мышление**.

## ✅ Критерии готовности

- [x] Все задачи `task01–task06` созданы и заполнены.
- [x] Техника перевода ошибок в доменные через `from error` соблюдена.
- [x] Есть список исключений, которые API обещает наружу.
- [x] Есть финальная таблица маппинга UX/exit codes.
- [x] Ты понимаешь ЧТО и ЗАЧЕМ делает каждый слой.

## 🛟 Устранение неполадок

- Если много ошибок — вероятно смешал зоны ответственности. Делай меньше, но чище.
- Если путаешься — просто рисуй: IO → parse → domain → boundary.
- Если появляется желание прятать ошибки внутри `print` → остановись. Только raise.
